// Re-export from @repo/validators — single source of truth
export type {
  FileResponse as FileRecord,
  FilesPaginatedResponse,
  PresignedUrlRequest,
  PresignedUrlResponse,
  ConfirmUploadInput,
} from "@repo/validators/files";
