// Stub — implement cell actions based on your data-table needs
export function CellActions({ children }: { children: React.ReactNode }) {
  return <div className="flex items-center gap-2">{children}</div>;
}
